import { ValidandoCepDirective } from './validando-cep.directive';

describe('ValidandoCepDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidandoCepDirective();
    expect(directive).toBeTruthy();
  });
});
